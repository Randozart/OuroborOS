/target
*.gguf
*.json
*.beast
*.o
*.so
*.dylib
__pycache__/
shards/
shards9b/
shards27/

# nix build symlinks
result
result-bin
result-man
enroll/

# WP-UPDATE signing key — the SEED never leaves the head and is never
# committed; the PUBLIC key is committed (it gets baked into images)
keys/update.signing.key

# BMTS shard payloads — content is reproducible from the GGUF via
# tools/shard_model.py + ouro-bmts upgrade; never commit gigabytes.
shards_bonsai27_n*/
*.duet-staging

# capnp C++ validation artifacts
capnp/*.c++
capnp/*.h
schemas/*.c++
schemas/*.h
